services:
  - type: web
    name: ppd
    runtime: static
    buildCommand: npm i -g pnpm@11.5.3 && pnpm install && pnpm run build
    staticPublishPath: ./dist
    pullRequestPreviewsEnabled: true
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: "true"
